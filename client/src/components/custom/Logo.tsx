import React from "react";

export default function Logo() {
  return (
    <span className="text-xl font-bold text-blue-600 tracking-tight select-none">
      {/* Replace with SVG or image if needed */}
      LMS
    </span>
  );
} 